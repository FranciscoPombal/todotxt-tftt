# Resolve the directory where this config file lives
CONFIG_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

export TODO_DIR="${TODO_DIR:-$CONFIG_DIR}"
export TODO_FILE="${TODO_FILE:-$TODO_DIR/todo/todo_debug.txt}"
export DONE_FILE="${DONE_FILE:-$TODO_DIR/done/done.txt}"
export REPORT_FILE="${REPORT_FILE:-$TODO_DIR/report/report.txt}"
